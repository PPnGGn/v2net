analyzer:
  errors:
    depend_on_referenced_packages: ignore
  exclude:
    # Pigeon contract source: non-nullable message fields have no initializer
    # by design; Pigeon validates it during generation.
    - pigeons/**
include: package:flutter_lints/flutter.yaml
